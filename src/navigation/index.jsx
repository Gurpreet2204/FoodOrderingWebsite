import { BrowserRouter } from "react-router-dom"
import { Header } from "../components/Header"

const Navigation=()=>{

    return(
        <BrowserRouter>
        <Header />
        </BrowserRouter>
    )
}